###### `It returns the country codes and names`
`

Run following command to install python modules:
`python3 -r requirment.txt`
`
`TinyDB is use in this project as the infile database. This is used to query the doc`